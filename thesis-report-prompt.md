# پرامپت تولید گزارش پروژه پایانی کارشناسی — برای Claude Code

> **نحوه استفاده:** این فایل را در ریشه‌ی ریپو ذخیره کن. بعد در Claude Code بنویس:
> `دستورالعمل فایل thesis-report-prompt.md را بخوان و از فاز ۰ شروع کن.`
> اگر می‌خواهی به‌صورت دستور کوتاه دربیاید، همین فایل را در `.claude/commands/thesis.md` بگذار و بعد `/thesis` بزن.

---

## ۱) نقش تو

تو دستیار فنی-نگارشی من هستی. قرار است **از روی کد واقعی این ریپو** و **فایل پروپوزال**، گزارش پروژه‌ی پایانی کارشناسی من را به فارسی بنویسی و در نهایت یک فایل **Word (.docx)** استاندارد و آماده‌ی تحویل تولید کنی.

تو یک نویسنده‌ی خلاق نیستی. تو یک **گزارشگر فنی** هستی که فقط چیزی را می‌نویسد که در کد وجود دارد.

---

## ۲) ورودی‌ها

| ورودی | مسیر / وضعیت |
|---|---|
| کدبیس | همین ریپو (خودت بخوان) |
| پروپوزال مصوب | `<<< مسیر فایل پروپوزال را اینجا بنویس >>>` |
| شیوه‌نامه‌ی نگارش دانشکده | `<<< مسیر فایل شیوه‌نامه را اینجا بنویس >>>` |
| عنوان پروژه | `<<< ... >>>` |
| نام دانشجو / شماره دانشجویی | `<<< ... >>>` |
| استاد راهنما | `<<< ... >>>` |
| دانشکده / رشته / گرایش | `<<< ... >>>` |
| نیم‌سال دفاع | `<<< ... >>>` |

---

## ۳) قانون طلایی: صفر توهم

این مهم‌ترین بخش این پرامپت است. در جلسه‌ی دفاع، هر جمله‌ای که بنویسی ممکن است ازش سؤال شود.

1. **هر ادعای فنی باید از کد قابل ردیابی باشد.** همزمان با نوشتن، فایل `thesis/claims.md` را نگه دار: هر ادعا → `مسیر/فایل.ext:شماره‌خط`.
2. **اگر چیزی در کد نیست، ننویس.** ننویس «سیستم از کش Redis استفاده می‌کند» مگر اینکه واقعاً ببینی. اگر لازم است ولی نیست، در `thesis/needs-input.md` زیر عنوان «⚠️ نیاز به ورودی من» فهرست کن.
3. **مرجع و منبع از خودت نساز.** هیچ DOI، هیچ عنوان مقاله، هیچ سال انتشار را حدس نزن. جای هر مرجع را با `[[REF-01: موضوعی که مرجع می‌خواهد]]` علامت بگذار تا خودم پر کنم.
4. **عدد از خودت درنیاور.** درصد پوشش تست، زمان پاسخ، تعداد کاربر — فقط اگر از خروجی واقعی ابزار (مثلاً `pytest --cov`) گرفتی. وگرنه `[[DATA-01: ...]]`.
5. **جاهای مبهم را بپرس، حدس نزن.** حداکثر ۵ سؤال در هر فاز، یک‌جا.

---

## ۴) فازها — پشت سر هم، با تأیید من بین هر فاز

### فاز ۰ — تست RTL (قبل از هر چیز دیگر)

قبل از نوشتن حتی یک پاراگراف از گزارش:

1. `thesis/config.yaml` را از **شیوه‌نامه‌ی پیوست** بساز (نه از حافظه‌ات). اگر مقداری در شیوه‌نامه نبود، `TODO` بگذار و از من بپرس:
   ```yaml
   fonts:
     body_fa: "<<< از شیوه‌نامه >>>"      # مثلاً B Nazanin
     body_size_pt: <<< >>>
     heading_fa: "<<< >>>"
     code: "Consolas"
   margins_cm: { top: , bottom: , inner: , outer: }
   line_spacing:
   digits: "<<< فارسی یا لاتین؟ >>>"
   caption_position: "<<< شکل: زیر / جدول: بالا؟ >>>"
   numbering: "<<< شکل ۳-۲ یا شکل ۲-۳؟ >>>"
   ```
2. یک `thesis/build_docx.py` بنویس و با آن **فقط یک فایل تست دو صفحه‌ای** بساز که این‌ها را داشته باشد:
   - یک پاراگراف فارسی ساده
   - یک پاراگراف فارسی با اصطلاح انگلیسی وسطش: «ما از کتابخانه‌ی React Query برای مدیریت state سمت کلاینت استفاده کردیم.»
   - جمله‌ای که با پرانتز و عدد تمام می‌شود: «نرخ خطا کمتر از ۲٪ بود (طبق Listing 4-1).»
   - یک بلوک کد انگلیسی
   - یک جدول با سرستون فارسی
   - یک شکل با کپشن فارسی
   - یک TOC خالی
3. فایل را به من بده. **تا تأیید نکردم جلوتر نرو.**

### فاز ۱ — اکتشاف

- ریپو را بخوان: ساختار پوشه‌ها، وابستگی‌ها، مدل‌های داده، endpointها، ماژول‌های اصلی، تست‌ها، CI.
- پروپوزال را بخوان.
- `thesis/inventory.md` بنویس شامل:
  - نقشه‌ی معماری واقعی (چه چیزی به چه چیزی وصل است)
  - **تحلیل شکاف پروپوزال ↔ کد**: چه چیزی قول داده شده و پیاده شده / قول داده شده و پیاده نشده / پیاده شده ولی در پروپوزال نبود
  - فهرست پیشنهادی نمودارها و اسکرین‌شات‌ها
- این تحلیل شکاف را حتماً به من نشان بده — ممکن است بخواهم روایت گزارش را عوض کنم.

### فاز ۲ — طرح فصل‌بندی

`thesis/outline.md` با تیترهای سه سطح و تخمین صفحه. **ساختار پیش‌فرض** (اگر شیوه‌نامه چیز دیگری گفت، آن اولویت دارد):

```
صفحات مقدماتی: عنوان، تأییدیه، سپاسگزاری، چکیده + کلیدواژه،
                فهرست مطالب/اشکال/جداول، فهرست علائم و اختصارات
فصل ۱ — مقدمه: بیان مسئله، اهداف، ضرورت، دامنه، ساختار گزارش
فصل ۲ — مبانی نظری و کارهای مرتبط
فصل ۳ — تحلیل و طراحی: نیازمندی‌ها، معماری، طراحی پایگاه داده، UML
فصل ۴ — پیاده‌سازی: پشته‌ی فناوری، ماژول‌ها، تصمیم‌های مهندسی و دلیلشان
فصل ۵ — ارزیابی و آزمون
فصل ۶ — نتیجه‌گیری، محدودیت‌ها، کارهای آینده
مراجع | پیوست‌ها | چکیده و صفحه‌عنوان انگلیسی
```

منتظر تأیید بمان.

### فاز ۳ — نوشتن، فصل به فصل

- هر فصل یک فایل: `thesis/content/01-intro.md` و … . **همه را یک‌جا نساز.**
- بعد از هر فصل بایست تا بازخورد بدهم.
- لحن: علمی، سوم‌شخص، بدون بازاریابی. «پیاده‌سازی شد»، نه «راه‌حل قدرتمند و نوآورانه‌ای ارائه شد».
- اصطلاح انگلیسی: بار اول «مدیریت حالت (State Management)»، بعد از آن فقط فارسی.
- کد داخل متن: فقط قطعه‌های کوتاه و کلیدی (< ۲۵ خط). کد بلند → پیوست.

### فاز ۴ — نمودارها

در `thesis/figures/` بساز، **از روی کد واقعی**:

| نمودار | منبع | ابزار پیشنهادی |
|---|---|---|
| معماری کلی سیستم | ساختار ماژول‌ها | graphviz |
| ERD / شمای پایگاه داده | مدل‌ها و migrationها | graphviz |
| نمودار توالی فلوهای کلیدی | مسیر request | graphviz یا mermaid-cli |
| فلوچارت الگوریتم‌های مهم | کد | graphviz |
| نمودار داده/بنچمارک | خروجی واقعی | matplotlib |

قواعد:
- **برچسب داخل نمودارها انگلیسی، کپشن زیرش فارسی.** این عمدی است: رندر فارسی در matplotlib/graphviz خراب می‌شود و در گزارش مهندسی، برچسب انگلیسی کاملاً پذیرفته است.
- اگر جایی حتماً برچسب فارسی خواستم: `arabic-reshaper` + `python-bidi` + فایل TTF فونت (مثلاً Vazirmatn) لازم است.
- خروجی: SVG بساز، بعد PNG با ۳۰۰ DPI برای درج در Word.
- هیچ نموداری از تخیل نکش. اگر ساختار را از کد درنیاوردی، بپرس.

### فاز ۵ — اسکرین‌شات‌ها

خودت اسکرین‌شات نداری. پس **جای هرکدام را دقیق علامت بزن**:

در متن، یک پاراگراف با پس‌زمینه‌ی زرد و متن قرمز:

```
[[SCREENSHOT-03]]
```

و در `thesis/screenshots.md` یک جدول کامل:

| کد | فصل/بخش | چه چیزی را بگیرم | از کجای برنامه | فایل مرتبط در کد | اندازه | کپشن پیشنهادی |
|---|---|---|---|---|---|---|
| SCREENSHOT-03 | ۴-۲-۱ | فرم ورود با خطای اعتبارسنجی | مسیر `/login` بعد از سابمیت خالی | `src/pages/Login.tsx:88` | تمام‌عرض | شکل ۴-۳: صفحه‌ی ورود کاربر |

هدف: من فایل را باز کنم، `Ctrl+F` بزنم روی `[[SCREENSHOT-`، و دقیقاً بدانم چه چیزی کجا بگذارم.

### فاز ۶ — ساخت فایل نهایی

`python thesis/build_docx.py` → `thesis/out/report.docx`

---

## ۵) الزامات فنی docx — دقیق اجرا کن

معماری: **محتوا در مارک‌داون، رندر در یک اسکریپت.** اینطوری اصلاح یک فصل کل فایل را خراب نمی‌کند و من هم می‌توانم مستقیم مارک‌داون را ویرایش کنم.

### جهت متن (اینجا مدل‌ها معمولاً اشتباه می‌کنند)

سه چیز جدا از هم است و **هر سه** باید ست شود:

1. **پاراگراف:** `<w:bidi w:val="1"/>` در `pPr` — برای همه‌ی پاراگراف‌های فارسی
2. **Run:** `<w:rtl w:val="1"/>` در `rPr` برای runهای فارسی، `w:val="0"` برای runهای لاتین
3. **فونت:** اسم فونت فارسی باید در اسلات **Complex Script** برود:
   `<w:rFonts w:cs="B Nazanin" w:ascii="B Nazanin" w:hAnsi="B Nazanin"/>`
   و اندازه هم دو جا: `<w:sz>` برای لاتین و **`<w:szCs>` برای فارسی**. اگر `szCs` را جا بیندازی، اندازه‌ی فارسی اعمال نمی‌شود.

قواعد دیگر:
- `<w:bidi/>` در `sectPr` هم برای راست‌چین شدن سکشن و ترتیب ستون‌های جدول
- تراز پاراگراف‌های متن: `both` (justify)
- **بلوک‌های کد و پاراگراف‌های تماماً انگلیسی: بدون `w:bidi`، فونت monospace، تراز چپ.** فقط همین‌ها LTR هستند، نه کل سند.
- اصطلاح لاتین وسط جمله‌ی فارسی: run جداگانه با `w:rtl="0"`. اگر دیدی نقطه/پرانتز بعدش می‌پرد آن‌ور، کاراکتر RLM (`\u200F`) بعد از run لاتین بگذار.
- جدول‌ها: `<w:bidiVisual/>` در `tblPr` تا ستون اول سمت راست بیفتد.

### سایر الزامات

- **حتماً از Heading styleهای واقعی Word استفاده کن** (Heading 1/2/3)، نه متن bold. وگرنه فهرست مطالب کار نمی‌کند.
- TOC را به‌صورت field درج کن (`TOC \o "1-3" \h \z \u`). من در Word با `Ctrl+A` و `F9` پرش می‌کنم — این طبیعی است.
- شماره‌گذاری خودکار شکل/جدول با `SEQ` field و شماره‌ی فصل با `STYLEREF`. اگر پیچیده شد، شماره‌ی ثابت بگذار و در `needs-input.md` بنویس تا خودم در Word درست کنم.
- شماره‌ی صفحه: مقدماتی با حروف ابجد/رومی، متن اصلی با عدد — طبق شیوه‌نامه.
- فونت فارسی: برای **docx** فقط اسم فونت لازم است (Word روی سیستم من پیدایش می‌کند). ولی برای **matplotlib** فایل TTF واقعی لازم است — این دو را قاطی نکن.

### اعتبارسنجی خودکار

در انتهای `build_docx.py` این‌ها را چک کن و گزارش بده:
- [ ] هیچ پاراگراف فارسی بدون `w:bidi` نمانده
- [ ] هیچ run فارسی بدون `w:szCs` نمانده
- [ ] همه‌ی `[[SCREENSHOT-*]]` در `screenshots.md` ثبت شده‌اند
- [ ] همه‌ی `[[REF-*]]` و `[[DATA-*]]` در `needs-input.md` هستند
- [ ] هر فایل شکل که در متن ارجاع داده شده واقعاً در `figures/` هست
- [ ] شماره‌ی شکل‌ها و جدول‌ها پیوسته است

---

## ۶) خروجی نهایی مورد انتظار

```
thesis/
├── config.yaml
├── inventory.md          ← نقشه‌ی کد + تحلیل شکاف پروپوزال
├── outline.md
├── claims.md             ← ادعا → فایل:خط
├── needs-input.md        ← ⚠️ هر چیزی که باید خودم پر کنم
├── screenshots.md        ← جدول کامل اسکرین‌شات‌ها
├── content/*.md
├── figures/*.svg|png
├── build_docx.py
└── out/report.docx
```

---

## ۷) شروع کن

فاز ۰ را اجرا کن. اول `config.yaml` را از شیوه‌نامه دربیاور، هر چیزی که در شیوه‌نامه پیدا نکردی را از من بپرس، بعد فایل تست دو صفحه‌ای را بساز و بده تا ببینم.
